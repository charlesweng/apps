import 'package:flutter/material.dart';

/// TODO:: Once playready sdk/ drm library for better player is ready.
class PlayreadyPlayer extends StatefulWidget {
  const PlayreadyPlayer({ Key? key }) : super(key: key);

  @override
  _PlayreadyPlayerState createState() => _PlayreadyPlayerState();
}

class _PlayreadyPlayerState extends State<PlayreadyPlayer> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}